#include "intérprete.h"

Linha rastilho_têr_linha_por_tipo(Tipo_Rastilho tipo) {
	devolve(nil)
}

Vago operação_re_definir(Integral operação_n, Expressão* expressão, Expectação expectação, Tipo_Operação operação_tipo, size_t linha_t) {
	devolve(nil)
}

Operação operação_falha() {
	devolve(nile(Operação))
}

Expressão* expressão_têr_por_ficha(Expressão* expressões, Linha ficha) {
	devolve(nil)
}

Expressão expressão_operação_têr_por_ficha(Expressão* expressão, Linha ficha) {
	devolve(nile(Expressão))
}

Expressão expressão_operação_têr_por_tipo(Expressão* expressão, Tipo_Operação tipo) {
	devolve(nile(Expressão))
}

Expressão expressão_falha() {
	devolve(nile(Expressão))
}

Vago intérprete_agregar(Intérprete ref intérprete, Expressão* expressões, Integral posição) {
	devolve(nil)
}

Integral intérprete_expressões_contar(const Intérprete* intérprete) {
	devolve(nil)
}

Vago interpretar_linha(Integral ref expressão_n, Linha ref linha, Intérprete ref intérprete) {
	devolve(nil)
}

Vago interpretar(Linha* ref linhas, Intérprete* intérprete) {
	devolve(nil)
}
